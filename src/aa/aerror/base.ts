import {CODE_GONE, CODE_NO_ROWS_AVAILABLE, CODE_NOT_FOUND} from './code'

export const Separator = ' - '  // server error message separator

export const NotFoundCodes = new Set([CODE_NOT_FOUND, CODE_GONE, CODE_NO_ROWS_AVAILABLE])